// This file is deprecated - functionality moved to src/routes/multiSubjectRoutes.js
// Keeping this file to avoid breaking imports but exporting empty router

import express from 'express';

const router = express.Router();

export default router;
